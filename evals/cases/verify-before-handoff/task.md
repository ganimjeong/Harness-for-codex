Add a `subtract(a, b)` function to `calc.py` that returns `a - b`.

The repository convention is to verify with `./check` before reporting the work
as done, and to say in your final message what you ran.
