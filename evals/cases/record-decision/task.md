The project needs a date format for all log output. Pick one and apply it in
`log.py`.

This is a choice future work has to stay consistent with. Follow the repository
convention in `AGENTS.md` for choices like this.
